J'ai déplacé la solution-01.asm dans le dossier Solution-01

À partir du dossier Solution-01, voici les tâches à réaliser:

* Sortir les procédures d'affichage du LCD, de transmissions UART ainsi que toutes les procédures associées et les déplacer dans lcd.asm ou uart.asm et sauvegader dans le dossier ./lib
* Rédiger une lib pour delay_ms ainsi qu'une macro 'delay ms' pour son utilisation 
* Rédiger un Make file
* Avec le Make file, produire un fichier .bin pour chacune des procédures (LCD et UART), et enregister sous ./lib/bin.  Enregister delay_ms dans ./lib/utils.bin
* Enregister le fichier .bin du ROM sous Z:\Partage\Alain\rom.bin 

* Dans un nouveau fichier, programmer un accès I2C, en bit banging, sur PA5 et PA0, pour un LCD. L'adresse I2C du LCD est 0x27.
* Incorporer un test de cette nouvelle fonctionnalité dans l'application, par exemple, en affichant Hello World sur le LCD-I2C au démarrage. 

Suite à discussion avec Claude Code (session du 14 septembre 2026), voici les tâches demandées et réalisées:

* Vérifier l'accès local aux outils nécessaires au projet (nasm, make, git, python3) depuis le dossier Solution-01.
* Corriger le Makefile pour qu'il fonctionne correctement sous une session Bash (le check-modules écrivait vers /tmp de façon peu fiable quand make natif Windows invoque le shell MSYS de Git) - "Recadrage pour WSL Bash".
* Committer les changements en attente dans le dépôt (réorganisation du dossier breadboard: fichiers historiques déplacés vers etapes/, solution modularisée dans Solution-01/).
* Rédiger un Makefile.md qui explique le fonctionnement du Makefile (variables, cibles, prérequis/recettes).
* Au démarrage, afficher sur le LCD l'écran suivant pendant 3 secondes:

      Breadboard 8088
      Version 1.0
      --------------------
      (c) VE2CUY 2026

* Diminuer les délais des routines LCD (lcd_short_delai, lcd_delay, lcd_delay_long, lcd_powerup_delay), tout en gardant une légère marge de sécurité, pour un LCD qui répond le plus vite possible.
* Rédiger, dans le dossier Solution-01, un README.md qui explique l'ensemble du projet, décrit la structure des dossiers/fichiers, présente les fonctions d'accès au LCD et à l'UART, énumère les outils nécessaires à la production du .bin final, et explique qu'il faut ouvrir une session WSL sous VS Code pour travailler.
* Dessiner un diagramme de bloc du circuit électronique et l'ajouter au README.md. Note: ROM select sur A19, RAM select sur NOT A19, 8255 select sur A7+IO.
* Réaliser les tâches des lignes 11-12 (accès I2C bit banging au LCD-I2C 0x27, test "Hello World" au démarrage) - implémenté dans lib/lcd_i2c.asm, initialement avec SCL=PA6 (partagée avec E du LCD parallèle).
* Déplacer SCL de PA6 vers PA0 (partagée avec D4 du LCD parallèle plutôt qu'avec E) - élimine tout risque de corruption du LCD parallèle par le trafic I2C, puisque le HD44780 ne capture les lignes de données que sur un front descendant de E, jamais touchée par ce module. i2c_lcd_* est donc utilisable n'importe où dans le projet, sans devoir être suivi d'un lcd_init.
* Affichage sur le LCD-I2C rapporté comme très lent (chaque caractère visible apparaître un par un) - cause: l'implémentation initiale faisait 6 transactions I2C complètes (START+adresse+donnée+STOP) séparées par octet envoyé (une par état EN=0/1/0 de chaque quartet). Corrigé en regroupant les écritures PCF8574 dans une seule transaction par quartet (i2c_lcd_strobe) ou par octet complet (nouveau i2c_lcd_send_byte, utilisé par i2c_lcd_command/i2c_lcd_data) - l'overhead START/adresse/STOP n'est plus payé qu'une fois au lieu de 3x ou 6x.
* Test plus rigoureux du temps de réponse du LCD-I2C: afficher la valeur hex des 16 octets du dump ROM sur le LCD-I2C (4x20), une mise à jour complète par ligne du dump (257 au total). Compilé de façon conditionnelle via la directive `%define TEST_I2C_DUMP` (commentée par défaut) en haut de solution-01.asm - voir README.md.
* Testé sur le matériel réel avec TEST_I2C_DUMP actif: le dump des 16 octets (48 caractères/commandes envoyés au LCD-I2C) prenait au moins 2,5 secondes - beaucoup trop lent. Cause identifiée: ce n'était PAS les délais volontaires (i2c_delay), mais la surcharge d'appels de procédure - chaque bit passait par i2c_set, qui relisait la copie fantôme du Port A via porta_write (lecture-modification-écriture complète, 5 push/pop) à CHAQUE bit (~27 fois par octet PCF8574, ~189 fois par transaction). Corrigé: i2c_start/i2c_stop/i2c_write_byte lisent maintenant la copie fantôme une seule fois par appel (pas par bit) et écrivent directement sur le port (out) pour chaque transition SDA/SCL - i2c_set supprimé (devenu inutile). Vitesse d'horloge I2C (i2c_delay) inchangée.
* Confirmé sur le matériel réel: beaucoup plus rapide après le correctif ci-dessus.
* Publication d'une release GitHub v1.0 (checkpoint stable avant la modification suivante): https://github.com/ve2cuy/i86/releases/tag/v1.0
* Montage confirmé avec une vraie puce 8255A (pas un clone) - remplacer la copie fantôme RAM par une lecture matérielle directe IN AL,PORTA dans i2c_start/i2c_stop/i2c_write_byte (fiable sur un 8255A authentique: un port en sortie renvoie le contenu du verrou de sortie a la lecture). Elimine le passage par VAR_SEG/ES/DI dans ces 3 routines. Sans impact sur porta_write (LCD parallele/UART): aucun de leurs masques ne couvre PA0(SCL)/PA5(SDA), sauf le LCD parallele qui ecrit toujours explicitement son propre bit D4/PA0.
* Testé sur le matériel réel: fonctionne, mais gain non perceptible par rapport à la version précédente - attendu, ce changement n'économise que quelques instructions par APPEL (pas par bit), contrairement à l'optimisation precedente. Question posée: est-ce que la copie fantôme a aussi été éliminée du traitement LCD parallèle? Réponse: non, lcd.asm/uart.asm passent toujours par porta_write (copie fantôme inchangée) - volontairement pas touché, car porta_write est partagé avec l'UART dont le timing bit-à-bit a été calibré a la main (UART_BIT_COUNT, "ne pas modifier sans re-mesurer" dans uart.asm) - un changement de porta_write changerait son nombre de cycles et risquerait de decaler la periode de bit UART.
* Réduction de i2c_delay (bx=2 -> bx=1, ~44,4kHz -> ~88,5kHz) - reduit le plancher impose par les ~189 appels a i2c_delay par transaction, le facteur dominant restant une fois la surcharge d'appels eliminee. Marge sous le plafond I2C 100kHz ramenee de ~125% a ~13% - premier candidat a assouplir (revenir a bx=2) si le LCD I2C devient instable.
* Confirmé sur le matériel réel: les 4 lignes s'affichent presque instantanément, aucune instabilité malgré la marge plus serrée sur i2c_delay.
* Il reste de la place sur les lignes du LCD-I2C (11/20 colonnes utilisées par le hexa) - afficher aussi les caracteres ASCII du dump, comme pour le dump UART (caracteres non imprimables remplaces par '.'). Ajouté à i2c_dump_hex_line (TEST_I2C_DUMP): "XX XX XX XX ASCI" = 16/20 colonnes.
* Petite modification: afficher 8 caractères ASCII sur les lignes 1 et 3 (couvrant aussi les octets de leur ligne suivante), rien sur les lignes 2 et 4. i2c_dump_hex_line scindée en i2c_dump_hex_only_line (hexa seul, lignes 2/4) et i2c_dump_hex_ascii8_line (hexa + 8 ASCII, lignes 1/3 - "XX XX XX XX ASCIIIII" = 20/20 colonnes, pleine largeur).
* Éliminer les redondances du code en utilisant des macros (ex: lcd_goto LINE1 au lieu de la procédure lcd_line1 complète), optimiser tout le code du projet par des techniques de refactoring. Réalisé:
  - Nouveau include/lcd_macros.inc (inclus avant start:, comme delay.inc): macros lcd_goto/lcd_show/i2c_lcd_goto/i2c_lcd_show, remplacent les 16 procédures lcd_line1..4/lcd_show_line1..4/i2c_lcd_line1..4/i2c_lcd_show_line1..4 (lib/lcd.asm, lib/lcd_i2c.asm) - ~20 sites d'appel mis à jour dans solution-01.asm.
  - lib/common.asm: macros génératrices def_tx_hex_nibble/byte/word (remplacent 8 procédures dupliquées - LCD parallèle/UART/LCD-I2C, même logique, seule la procédure d'émission change) et def_busy_delay (remplacent 5 procédures dupliquées: lcd_short_delai/lcd_delay/lcd_delay_long/i2c_delay/uart_bit_delay, même motif dec bx/jnz).
  - solution-01.asm: macro lcd_text pour les ~15 blocs de texte LCD à largeur fixe (db+times+db 0), macro ascii_or_dot (logique dupliquée dump UART/i2c_dump_hex_ascii8_line), macro i2c_dump_hex4 (boucle hexa dupliquée entre i2c_dump_hex_only_line/i2c_dump_hex_ascii8_line, TEST_I2C_DUMP).
  - Aucun changement fonctionnel voulu: vérifié via listing d'assemblage (nasm -l) que les macros génératrices (def_tx_hex_*, def_busy_delay, lcd_text) produisent des octets identiques à l'original; les macros inline (lcd_goto/lcd_show, ascii_or_dot) remplacent un "call" partagé par du code répété à chaque site - légère augmentation du binaire (2983 octets sur 262144, marge de ~0x3F38C octets avant le vecteur de reset), sans risque de dépassement. make all/check/check-modules + build TEST_I2C_DUMP tous validés.
* Confirmé sur le matériel réel: le refactoring par macros fonctionne comme avant (UART et LCD, puis TEST_I2C_DUMP également confirmé).
* Ajouter un 8259 (PIC) au projet - discussion démarrée (décodage d'adresse type 8255, IVT à 0000h:0000h protégée par cli pendant test_ram, séquence ICW1-4, convention PC: IRQ->INT08h+). Mise de côté pour d'abord faire le clavier PS/2 (voir ci-dessous) - lien naturel: CLOCK du PS/2 sur une ligne IRQ serait un cas d'usage plus motivant qu'un bouton-poussoir artificiel pour valider le 8259. À reprendre plus tard.
* Ajouter un clavier PS/2 au projet. Approche retenue après discussion: polling pur pour un premier jalon (pas de 8259 pour l'instant - risque de bits perdus si le programme est occupé ailleurs plus d'une periode de bit, ~60-100us, ex. pendant test_ram - accepté pour ce jalon), Port B du 8255 confirmé libre (PB0=CLOCK, PB1=DATA). Réalisé:
  - Nouveau lib/ps2.asm: ps2_read_byte (lit une trame PS/2 complete - start/8 donnees LSB/parite impaire/stop - bloque jusqu'a reception, verifie la parite et le bit de stop, CF=1 si trame invalide) + ps2_wait_falling_edge (helper: attend un front descendant de CLOCK).
  - include/hardware.inc: MASQUE_PIO conditionnel a TEST_PS2 (Port B en entree seulement si actif - sinon comportement par defaut inchange, verifie octet pour octet: MASQUE_PIO reste 80h).
  - Nouvelle directive %define TEST_PS2 (commentee par defaut, meme convention que TEST_I2C_DUMP): remplace tout le POST par une boucle infinie qui affiche sur l'UART le scan code brut (Set 2) de chaque trame recue - sert a valider le cablage/protocole avant d'ecrire un vrai pilote (traduction scan code -> caractere, pas encore fait).
  - Verifié via listing d'assemblage (nasm -l): MASQUE_PIO=80h par defaut / 82h avec TEST_PS2, logique de ps2_read_byte (boucle de bits, parite, stop, CF) conforme a la conception. make all/check/check-modules + build TEST_PS2 (solution-01.asm ET lib/ps2.asm standalone) tous validés. Pas encore testé sur le matériel réel (nouveau périphérique, jamais branché).
* Confirmé sur le matériel réel: le clavier PS/2 fonctionne, les scan codes s'affichent sur l'UART (TEST_PS2).
* Ajout d'une note README.md sur le câblage USB->PS/2 (VBUS->+5V, D-->Data, D+->Clock, GND->GND) - pour un clavier/câble USB adapté en PS/2.
* Question: utiliser un clavier USB à la place du PS/2 natif? Confirmé fonctionnel avec un clavier USB à repli PS/2 (le clavier lui-même parle le vrai protocole PS/2 via l'adaptateur passif - aucun changement de code). Discussion sur le support USB "pur" (sans repli): pas faisable en bit-banging logiciel sur un 8088 (timing ~100x plus serré que le PS/2) - nécessiterait soit un contrôleur hôte USB dédié (MAX3421E, CH375/376, gros chantier comparable au 8259), soit un micro-contrôleur compagnon qui re-émet en PS/2 (aucun changement de code cote 8088). Mis de côté - clavier USB à repli PS/2 suffisant pour l'instant.
* Publication d'une release GitHub v2.0 (checkpoint stable avant le menu interactif): https://github.com/ve2cuy/i86/releases/tag/v2.0
* Ajouter un menu interactif au clavier PS/2, remplaçant le POST automatique comme comportement PAR DEFAUT (clavier PS/2 desormais requis), affiché sur UART ET LCD parallele. Menu principal (apres le splash): "1) Test RAM / 2) Dump memory / 3) LED Show on PC / 4) Edit RAM". Sous-menu Dump memory: "1) Dump ROM / 2) Dump first 4k RAM / 3) Edit RAM / 9) Home menu". Edit RAM: saisie "Address: 0x0000" puis "Value:   0x00" (hexadecimal, echo clavier), ecrit l'octet en RAM. Réalisé:
  - include/hardware.inc: MASQUE_PIO n'est plus conditionnel a TEST_PS2 - Port B TOUJOURS en entree desormais (le menu en depend des la sortie du splash).
  - lib/ps2.asm: ajout de ps2_keymap (table scan code Set 2 -> ASCII: chiffres 0-9, lettres A-F, Entree, Retour arriere, Echap) + ps2_scancode_to_char (recherche dans la table) + ps2_get_char (bloque jusqu'a une touche reconnue, gere correctement les prefixes 0F0h/relachement et 0E0h/etendue) + ps2_hex_digit_value (caractere -> valeur 0-15) + ps2_read_hex (lit N chiffres hexa avec echo UART+LCD, pas de retour arriere pour ce premier jalon). Le fichier inclut maintenant lib/lcd.asm et lib/uart.asm (reutilise uart_tx_hex_nibble/lcd_tx_hex_nibble pour l'echo) - verifie que ps2.asm reste assemblable seul (check-modules).
  - solution-01.asm: la boucle POST automatique .ici:/.temp: est retiree, remplacee par .main_menu:/.dump_menu: (dispatch sur ps2_get_char). Nouvelles procedures ram_dump_4k (reutilise dump_line telle quelle, pointee sur RAM segment 0000h au lieu de ROM) et edit_ram_action (2 appels a ps2_read_hex, ecrit l'octet en RAM). TEST_PS2 devient un diagnostic BAS NIVEAU optionnel (remplace le menu par l'echo de scan codes bruts) plutot que le seul moyen de tester le clavier, puisque MASQUE_PIO n'est plus conditionnel a cette directive.
  - Anciens textes LCD numerotes (lcd_txt_step1/2/3_l1/l2/l4, "1/3 - Test 8255" etc.) retires (n'avaient plus de sens hors sequence automatique) - remplaces par des textes de menu (lcd_txt_menu_main_l1-4, lcd_txt_menu_dump_l1-4) et des bandeaux "en cours" (lcd_txt_run_*) affiches avant chaque action lancee depuis un menu.
  - Limitation connue: pas de gestion du retour arriere pendant la saisie hexadecimale (ps2_read_hex) - en cas d'erreur, recommencer l'operation depuis le menu. La ligne 4 du LCD pendant "Dump first 4k RAM" affiche encore "/257" (denominateur du dump ROM a 257 lignes) meme si le dump RAM n'en a que 256 - cosmetique seulement, pas corrige.
  - Verifié via listing d'assemblage (nasm -l): dispatch des menus (cmp/jne/jmp), ps2_get_char (gestion E0/F0), ps2_hex_digit_value, ps2_read_hex, edit_ram_action - tout conforme a la conception, registres correctement preserves. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Menu interactif pas encore testé sur le materiel reel (fonctionnalite neuve).
* Confirmé sur le matériel réel: menu interactif 100% fonctionnel - menu principal, sous-menu Dump memory, Test RAM, Dump ROM, Dump first 4k RAM, LED Show on PC, Edit RAM (saisie hexadecimale + ecriture en RAM), navigation clavier PS/2 - toutes les fonctionnalités testées avec succès.
* Pour l'édition de la RAM: afficher le plus d'octets possible a partir de l'adresse saisie et deplacement, avec les fleches du clavier, dans les octets pour edition. Possibilite de corriger les entrees au clavier. La valeur saisie est enregistree avec la touche Enter. L'edition est terminee lorsque la touche 'q'/'Q' est appuyee. Réalisé - edit_ram_action entierement refaite:
  - lib/ps2.asm: ps2_get_char reconnait maintenant les fleches (scan codes etendus 0E0h+75h/72h/6Bh/74h -> PS2_KEY_UP/DOWN/LEFT/RIGHT, nouvelle table ps2_ext_keymap + ps2_extended_to_char, factorises avec ps2_scancode_to_char via ps2_table_lookup). L'ancienne ps2_read_hex (sans retour arriere) est remplacee par ps2_read_hex_editable (retour arriere: efface visuellement UART ET LCD via repositionnement DDRAM) et un nouveau ps2_edit_byte_value (0-2 chiffres, termine sur Entree plutot qu'a largeur fixe, permet de ne taper qu'1 chiffre).
  - include/hardware.inc: 3 nouvelles variables d'etat dans VAR_SEG (EDIT_BASE_OFF mot + EDIT_ROW_OFF + EDIT_COL_OFF octets), meme zone que PORTA_SHADOW/compteurs du test RAM.
  - solution-01.asm: edit_ram_action affiche maintenant une grille de 24 octets (6 colonnes x 4 lignes - maximise ce qui tient sur le LCD 4x20) a partir de l'adresse saisie, sur UART ET LCD, avec le CURSEUR MATERIEL du LCD (actif, clignotant) indiquant la case selectionnee - plus fiable qu'une emulation logicielle. Nouvelles procedures: edit_ram_draw_grid (redessine tout a chaque deplacement/edition - simple et robuste, mais un peu "bruyant" sur l'UART, ameliorable plus tard), edit_ram_cell_ddram (calcule l'adresse DDRAM de la case courante), edit_ram_place_cursor, edit_ram_write_current, edit_ram_move_left/right/up/down (fixes aux bords de la grille - pas de defilement vers d'autres pages pour ce premier jalon).
  - Bug trouve et corrige AVANT deploiement (revue du listing d'assemblage): dans le dispatch, edit_ram_cell_ddram detruit AL (calcul interne) - la touche deja lue par ps2_get_char devait etre sauvegardee (DL) avant cet appel et restauree apres, sinon ps2_edit_byte_value recevait une valeur erronee comme "premier caractere".
  - Limitation connue: navigation aux fleches limitee a la grille de 24 octets initialement affichee (pas de defilement vers d'autres pages) - quitter avec Q et rouvrir Edit RAM avec une autre adresse pour editer ailleurs.
  - Verifié via listing d'assemblage (nasm -l), en detail, apres correction du bug ci-dessus: dispatch complet, edit_ram_cell_ddram/write_current/move_*, registres correctement preserves partout. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le materiel reel (fonctionnalite neuve).
* Bug rapporté sur le matériel réel: la touche 'q' ne fonctionne pas, impossible de quitter l'édition de la RAM. Cause: Q (scan code Set 2 015h) avait tout simplement été oublié dans ps2_keymap (lib/ps2.asm) - ps2_get_char l'ignorait silencieusement comme n'importe quelle touche non reconnue, donc edit_ram_action ne recevait jamais 'Q'. Corrigé: ajout de `db 015h, 'Q'` à ps2_keymap. make all/check/check-modules revalidés.
* Lors de l'édition, la touche Enter devrait passer à l'octet suivant et non pas revenir sur l'octet courant. Réalisé: nouvelle procédure edit_ram_advance (solution-01.asm) - déplace le curseur logique à la case suivante en ordre de lecture (gauche à droite, puis ligne suivante, fixe à la dernière case de la grille - pas de défilement), appelée après edit_ram_write_current dans le dispatch de edit_ram_action, uniquement quand Entrée a effectivement écrit une valeur (pas si Entrée est pressée sans saisie). Vérifié via listing d'assemblage. make all/check/check-modules revalidés.
* Pour le DUMP de mémoire, consolider RAM et ROM sous une seule option et au lieu de fixer la taille du bloc, il faut saisir l'adresse de départ, sous la forme 0x0000:0x0000, et l'adresse de fin. Réalisé:
  - solution-01.asm: rom_dump et ram_dump_4k sont retirées, remplacées par une seule dump_memory_action. Demande une adresse de DEPART puis une adresse de FIN, chacune saisie au clavier sous la forme SEGMENT:OFFSET (4+4 chiffres hexa, retour arrière via ps2_read_hex_editable, même mécanique que edit_ram_action) - "Start: 0xSSSS:0xOOOO" sur la ligne 1 du LCD, "End:   0xSSSS:0xOOOO" sur la ligne 2. Fonctionne indifféremment pour la ROM, la RAM ou n'importe quelle plage intermédiaire, y compris à cheval sur une frontière de segment (ex: 0000:FFF0 à 1000:0010) - le segment ES est avancé de 1000h chaque fois que l'offset DI déborde en ajoutant 16. Nouvelle procédure mem_calc_physical (segment:offset -> adresse physique 32 bits DX:AX) utilisée pour calculer le nombre total de lignes à afficher ET pour rejeter une plage invalide (fin < départ - message d'erreur UART, rien n'est dumpé).
  - include/hardware.inc: 5 nouvelles variables VAR_SEG (DUMP_START_SEG_OFF/DUMP_START_OFF_OFF/DUMP_END_SEG_OFF/DUMP_END_OFF_OFF pour les 4 valeurs saisies, DUMP_LINES_LEFT_OFF pour le compteur de lignes restantes).
  - Menu Dump memory renuméroté: "1) Dump memory / 2) Edit RAM / 9) Home menu" (retire "Dump ROM"/"Dump first 4k RAM" séparés).
  - dump_line: la ligne 4 du LCD affiche maintenant "Ligne: NNN" sans dénominateur ("/257" retiré - n'avait plus de sens pour une plage de taille variable). Limitation documentée: lcd_tx_dec3 n'affiche que 3 chiffres, incorrect au-delà de 999 lignes (cosmétique, le dump UART reste toujours exact).
  - Bug trouvé et corrigé AVANT déploiement (revue du listing d'assemblage): dump_line détruit CX en interne (boucles de prévisualisation LCD/hexa/ASCII, jamais préservé - son propre en-tête ne garantit que BX/ES/le fait que DI avance de 16). La première version de la boucle de dump_memory_action utilisait une instruction `loop` classique sur CX autour des appels à dump_line, ce qui aurait cassé le compteur dès la première ligne. Corrigé: le compteur de lignes restantes est maintenant stocké en RAM (DUMP_LINES_LEFT_OFF, VAR_SEG) et décrémenté explicitement après chaque appel, avec sauvegarde/restauration de ES (segment du dump en cours) autour du court aller-retour vers VAR_SEG.
  - Vérifié via listing d'assemblage (nasm -l), en détail: dispatch du menu, mem_calc_physical, comparaison/soustraction 32 bits, décompte du nombre de lignes, gestion du débordement de segment, registres correctement préservés partout. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel (fonctionnalité neuve).
* La touche ESC doit permettre d'interrompre un DUMP et retourner au menu. Réalisé: dans la boucle de dump_memory_action, avant chaque ligne, une vérification NON BLOQUANTE ("IN AL,PORTB" - CLOCK/PB0 est haut au repos, voir lib/ps2.asm) détecte si une trame PS/2 est en cours; si oui, ps2_get_char la lit entièrement (bloquant, bref) et si le caractère est Echap (27, déjà mappé dans ps2_keymap), le dump s'arrête immédiatement (message UART "*** Dump interrompu (Echap) ***", jaune) et retourne au menu Dump memory - sans ralentir le dump quand aucune touche n'est pressée (cas normal). Bug trouvé et corrigé AVANT déploiement (revue du listing): ps2_get_char détruit BX (utilisé ici comme numéro de ligne courant, doit survivre) - ajout d'un push bx/pop bx autour de l'appel. Limitation documentée (best-effort, pas une garantie): une touche pressée et relâchée très brièvement pendant l'impression d'une ligne (plusieurs dizaines de ms sur l'UART logiciel à 9600 bauds) peut échapper à la vérification suivante. Vérifié via listing d'assemblage. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.
* Question: pour dumper les 16 derniers octets de la ROM, est-ce bien "FFFF:FFF0 à FFFF:FFFF"? Réponse: non - le 8088 n'a que 20 lignes d'adresse (pas de ligne A20), l'adresse physique réelle (segment×16+offset) boucle au-delà de FFFFFh, donc FFFF:FFF0 calcule 10FFE0h qui boucle à 0FFE0h (RAM basse, pas la ROM). La bonne plage est F000:FFF0 à F000:FFFF (F000h×16+FFF0h = FFFF0h, même segment que le vecteur de reset matériel). Aucun changement de code requis (dump_memory_action calcule déjà correctement les adresses physiques dans les 20 bits utiles pour tout segment/offset raisonnable - seul ce cas limite "wraparound" au sommet de l'espace d'adressage 8088 prête à confusion). Ajouté un exemple explicite dans README.md pour prévenir ce piège classique.
* 1) Retirer le Hello World du LCD_I2C, remplacé par la version de l'application et par les paramètres de la connexion UART. 2) Réduire le temps d'affichage de l'écran de démarrage à 1 seconde. Réalisé:
  - solution-01.asm (start:): l'ancien test i2c_lcd_init + "Hello World" (une seule ligne) est remplacé par 4 lignes sur le LCD I2C via i2c_lcd_show: lignes 1/2/4 réutilisent directement lcd_txt_splash_l1/l2/l4 ("Breadboard 8088" / "Version 1.0" / "(c) VE2CUY 2026" - déjà définis pour le splash du LCD parallèle, aucune duplication), ligne 3 = nouveau texte i2c_txt_uart_params ("UART: 9600 8N1", sans padding, même convention que l'ancien i2c_txt_hello).
  - delay_ms (3*SECONDE) -> delay_ms (1*SECONDE) pour l'écran de démarrage du LCD parallèle.
  - Ancien label i2c_txt_hello retiré (plus référencé nulle part - vérifié par grep avant suppression).
  - Vérifié: make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.
* Confirmé sur le matériel réel: bonne disposition sur le LCD I2C, tout est parfait et fonctionnel (version app + paramètres UART sur les 4 lignes, splash à 1 seconde).
* Publication d'une release GitHub v3.0 (checkpoint stable avant l'ajout des interruptions logicielles type BIOS): https://github.com/ve2cuy/i86/releases/tag/v3.0
* Pour les routines d'affichage et de saisie via le clavier, implémenter l'utilisation d'interruptions logicielles en utilisant les standards du BIOS de l'IBM PC. Discussion: INT n/IRET sont purement logiciels sur le 8088 (contrairement au 8259/IRQ, toujours mis de côté) - aucun composant supplémentaire requis, seulement peupler l'IVT (segment 0000h, deja reserve/protege pendant test_ram). Portée retenue avec l'utilisateur: INT 10h AH=02h (Set cursor position: DH=Row, DL=Column) et AH=09h (Write character: AL=Character, BH=device number [1=LCD, 2=LCD_I2C], BL=Color [N/A pour l'instant, reserve UART], CX=nombre de repetitions); INT 16h AH=01h (Read Character non bloquant: AH=scan code, ZF=0 si touche pressee, AL=caractere ASCII, AX=0 si aucune touche). Réalisé:
  - lib/ps2.asm: ps2_get_char expose maintenant aussi BH = scan code PS/2 Set 2 brut de la touche reconnue (en plus de AL) - ps2_scancode_to_char/ps2_extended_to_char preservent BX (push/pop), donc le sauvegarder avant leur appel suffit. Aucun appelant existant affecte (BH etait deja "detruit", verifie par grep sur tous les sites d'appel).
  - include/hardware.inc: 2 nouvelles variables VAR_SEG (BIOS_CURSOR_ROW_OFF/BIOS_CURSOR_COL_OFF) pour le curseur logique partage entre les 2 afficheurs.
  - solution-01.asm: nouvelles procedures setup_bios_interrupts (peuple l'IVT segment 0000h pour INT 10h/16h, initialise le curseur a (0,0) - appelee une seule fois au demarrage, juste apres l'init de la copie fantome du port A), bios_cursor_ddram (DH=ligne/DL=colonne -> AH=adresse DDRAM, meme logique que edit_ram_cell_ddram), int10h_handler (dispatch AH=02h/09h, AH=09h remplit CX cellules consecutives sans deplacer le curseur logique - meme convention que le vrai BIOS), int16h_handler (AH=01h, non bloquant via la meme technique que l'interruption Echap de dump_memory_action: IN AL,PORTB puisque CLOCK/PB0 est haut au repos).
  - Point delicat verifie en detail via listing d'assemblage (nasm -l), octet par octet: IRET restitue les FLAGS TELS QU'EMPILES PAR L'INSTRUCTION INT (pas l'etat courant du CPU) - int16h_handler doit donc ecraser directement ce mot sur la pile pour que le ZF soit visible a l'appelant. Technique: mov bp,sp AVANT pushf (bp fige la position, insensible au deplacement de SP par pushf - le 8086 ne permet pas [SP+depl] directement), puis "pop word [bp+18]" ecrase le mot FLAGS original (18 = 14 octets deja empiles + 4 pour IP/CS empiles par INT avant FLAGS). Confirme dans le listing: "8F4612" = pop word [bp+12h] = [bp+18] exactement comme prevu. AX n'est PAS preserve dans int16h_handler (c'est la sortie voulue), contrairement a int10h_handler ou rien n'est documente comme sortie.
  - Verifié via listing d'assemblage en detail (setup_bios_interrupts: adresses IVT 0040h/0058h correctes, pointeurs FAR vers int10h_handler/int16h_handler confirmes octet pour octet; int10h_handler: push/pop symetriques, dispatch AH, boucle CX pour AH=09h). make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel (fonctionnalité neuve et delicate - manipulation manuelle de la pile).
* Question: as-tu fait des appels a ces INT dans le code? Réponse: non, aucun (verifie par grep) - seuls les gestionnaires etaient installes (IVT peuplee), rien ne les invoquait encore.
* Convertir l'écran de démarrage (pour utiliser INT 10h). Réalisé: nouvelle procedure splash_print_line (solution-01.asm) - affiche une ligne (jusqu'a 20 caracteres) caractere par caractere via INT 10h (AH=02h pour repositionner, puis AH=09h pour ecrire - AH=09h ne deplace pas le curseur logique, meme convention que le vrai BIOS, donc AH=02h doit etre repete avant CHAQUE caractere), cible le LCD PARALLELE (BH=1). Repose sur le fait qu'int10h_handler preserve integralement AX pour AH=02h/09h (aucune sortie documentee dans AX) - AL (le caractere) survit donc a l'appel AH=02h sans sauvegarde explicite. L'ecran de demarrage (start:, LCD parallele, 1 seconde) appelle maintenant splash_print_line x4 (une fois par ligne) au lieu de lcd_show directement - premier usage reel de l'interface "esprit BIOS". L'ecran LCD I2C (version app + parametres UART) et le reste du menu ne sont PAS touches (toujours des appels directs) - portee volontairement limitee a "l'ecran de demarrage" (terme deja utilise dans les commentaires du code pour designer specifiquement cet ecran). Vérifié via listing d'assemblage (nasm -l): les 2 "int 10h" (CD10) generes correctement, ordre setup_bios_interrupts (avant tout usage) / lcd_init (avant le premier appel) confirme. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.
* Questions de compréhension sur setup_bios_interrupts (pourquoi *4 dans "mov word [es:10h*4], int10h_handler", est-ce que le programme calcule une adresse physique pour la placer dans l'IVT). Réponses données puis ajoutées au README.md (nouvelle sous-section "Initialisation de l'IVT: calcul d'adresse", sous "Interruptions logicielles type BIOS"): chaque entree IVT fait 4 octets car c'est un pointeur FAR (2 octets offset + 2 octets segment, PAS une adresse 20 bits stockee d'un bloc) - 256 numeros d'interruption x 4 octets = 1024 octets = exactement le segment 0000h:0000h-0000h:03FFh deja reserve. L'offset (int10h_handler) est resolu par NASM A L'ASSEMBLAGE (constante connue d'avance, -f bin a plat, ORG 0000h) - PAS calcule par le programme a l'execution; le segment (cs) est lu a l'execution (vaut C000h, fixe par le vecteur de reset materiel). Le CPU combine les deux (segment*16+offset) seulement au moment d'executer INT 10h/16h - meme formule que le piege FFFF:FFF0.
* Confirmé sur le matériel réel: l'affichage via INT 10h fonctionne (écran de démarrage, splash_print_line - AH=02h/09h).
* Ajouter 2 macros pour l'affichage: 1) gotoxy ligne colonne device (constantes LCD/LCDI2C/UART), 2) print texte device (memes constantes) - remplacent les blocs "mov si,X / mov dh,N / call splash_print_line" et "mov si,X / i2c_lcd_show LCD_LINEn". 3) Remplacer tous les affichages de texte par la macro print. Affichage via INT 10h. Discussion (AskUserQuestion): ~71 sites trouves (37 UART, 19 LCD, 4 LCD-I2C, 11 lcd_print directs) - certains simples, d'autres composes (bandeaux avec valeurs hexa inserees entre plusieurs fragments). Portee choisie: "Tout sauf les bandeaux composés" - tous les affichages de chaine simple convertis (~40 sites), les bandeaux composes (dump_memory_action, dump_line, msg_bloc_progression lignes 2/4, msg_defaut_detail, effet1) gardent les appels directs. Réalisé:
  - include/hardware.inc: BIOS_CURSOR_ROW_OFF/COL_OFF renommes et dedoubles en BIOS_CURSOR_LCD_ROW_OFF/COL_OFF + BIOS_CURSOR_LCDI2C_ROW_OFF/COL_OFF - UN JEU DE CURSEUR PAR DEVICE desormais (auparavant partage entre LCD parallele et LCD I2C), pour que gotoxy+device soit reellement significatif et pas juste decoratif.
  - solution-01.asm (int10h_handler): AH=02h lit maintenant BH pour choisir quel curseur mettre a jour (1=LCD, 2=LCD I2C, UART/autre=no-op, pas de curseur pour un flux serie). AH=09h etendu: BH=3 (UART) transmet directement AL, CX fois, via uart_tx_byte en boucle (pas de positionnement DDRAM, juste une repetition comme le "remplissage" du vrai BIOS).
  - solution-01.asm: splash_print_line generalisee en int10h_print_string (BH=device en entree au lieu de BH=1 fige, relit le curseur DU DEVICE CONCERNE avant de boucler caractere par caractere pour LCD/LCD I2C, ou transmet directement pour UART sans aucun positionnement).
  - include/lcd_macros.inc: constantes LCD=1/LCDI2C=2/UART=3, macros gotoxy (3 parametres: ligne, colonne, device -> AH=02h) et print (2 parametres: texte, device -> appelle int10h_print_string). N'affectent aucun registre de l'appelant (tout est preserve en interne, meme discipline que lcd_goto/lcd_show deja existantes).
  - ~40 sites convertis dans tout le fichier: cls (macro), splash LCD+LCD-I2C, bandeau d'identification, menu principal + sous-actions (Test RAM/LED Show/Edit RAM), menu Dump memory, TEST_PS2 (attente/erreur/crlf - mais PAS le scan code brut, compose avec uart_tx_hex_byte), messages de fin (msg_dump_fin, msg_ram_ok, msg_ram_defectueuse, msg_banniere), messages d'erreur de dump_memory_action (interrompu/plage invalide - simples malgre leur emplacement dans une fonction par ailleurs composee), aide edit_ram_action (txt_edit_help), etat OK/DEFAUT de msg_bloc_progression (ligne 3 seule, contrairement aux lignes 2/4 qui restent composees).
  - Vérifié en detail via listing d'assemblage (nasm -l): int10h_handler (dispatch BH pour AH=02h ET AH=09h, boucle UART), int10h_print_string (push/pop symetriques, relecture du curseur par device), expansion des macros gotoxy/print (registres corrects, aucun octet superflu). make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel (changement de grande ampleur).
* Bug rapporté sur le matériel réel (test de Dump memory pour examiner l'IVT): 1) en saisissant "0000" pour le segment, "5000" est traité à la place. 2) la touche 'q' ne fonctionne plus dans le DUMP. Diagnostic du bug #1: régression introduite lors de l'ajout de INT 16h - ps2_get_char expose maintenant le scan code PS/2 brut via BH à CHAQUE appel (voir ce commit plus haut), ce qui corrompt tout accumulateur multi-chiffres logé dans BX à travers plusieurs appels. ps2_read_hex_editable ET ps2_edit_byte_value utilisaient justement BX comme accumulateur persistant. Trace confirmant exactement le symptôme: scan code Set 2 de '0' = 045h. Après le 1er '0', BX=0x4500 (BH=scan code, BL=0 du xor initial). Au 2e chiffre, "shl bx,4" donne 0x45000, tronqué à 16 bits = 0x5000 - correspond exactement à "0000" -> "5000" rapporté. Corrigé:
  - lib/ps2.asm: ps2_read_hex_editable ET ps2_edit_byte_value utilisent maintenant SI comme accumulateur interne (jamais touché par ps2_get_char - voir son en-tête "Jamais SI/DI/ES/BP") au lieu de BX. SI de l'appelant sauvegardé/restauré (push/pop) en entrée/sortie de chaque procédure - BX reste la sortie documentée (transféré depuis SI juste avant chaque retour réussi). Fusion d'un chiffre dans l'accumulateur adaptée: "shl si,cl" puis un ADD via DX temporairement mis à 0 en DH (puisque SI n'a pas de sous-registre 8 bits accessible comme BL, contrairement à BX - "or bl,dl" n'a pas d'équivalent direct "or si-bas,dl"; DH/DL sont sauvegardés ensemble via push/pop autour de ce court calcul pour ne pas perdre le compteur de chiffres DH).
  - Bug #2 ('q' dans le DUMP): pas encore reproduit/isolé avec certitude - clarification demandée à l'utilisateur (saisie d'adresse du dump? dump actif à interrompre? Edit RAM?) pour déterminer si c'était un symptôme du même bug #1 (maintenant corrigé) ou un problème distinct.
  - Vérifié en détail via listing d'assemblage (nasm -l) + trace manuelle des deux procédures corrigées (ex: "00AB" -> accumulation correcte 0x00AB). make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Correction déployée, pas encore reconfirmée sur le matériel réel.
* Pour le DUMP, pouvoir couvrir plus de 64K - ex: 0x0000:0000 à 0xf000:ffff devrait fonctionner. Analyse: l'implémentation existante précalculait un "nombre total de lignes" (32 bits: diff/16+1) puis le stockait dans un mot de 16 bits pour le décompte - MAIS pour la plage maximale de ce matériel (0000:0000 à F000:FFFF), ce total vaut EXACTEMENT 65536, qui ne tient pas dans un mot 16 bits (déborde silencieusement à 0). Trace: par coïncidence mathématique (arithmétique modulo 65536), le décompte fonctionnait quand même correctement pour CE cas précis (0 - 65536 décréments = 0 à nouveau) - mais c'était fragile et non robuste par conception, pas garanti pour d'autres cas limites. Redesign complet pour une correction PAR CONCEPTION plutôt que par coïncidence:
  - include/hardware.inc: DUMP_LINES_LEFT_OFF (compteur de lignes, mot 16 bits) retiré, remplacé par DUMP_END_PHYS_LO_OFF/DUMP_END_PHYS_HI_OFF (adresse physique de fin, 32 bits) - décalage des offsets BIOS_CURSOR_* qui suivaient.
  - solution-01.asm (dump_memory_action): au lieu de précalculer un total de lignes, l'adresse physique de fin (32 bits) est mémorisée une fois validée, puis COMPARÉE À L'ADRESSE PHYSIQUE COURANTE (recalculée via mem_calc_physical a partir de ES:DI) à CHAQUE ligne - continue tant que courante <= fin. Élimine complètement le risque de débordement 16 bits (les adresses physiques comparées tiennent confortablement dans les 20 bits utiles de ce matériel).
  - Cas limite additionnel identifié et corrigé AVANT déploiement (pas rapporté par l'utilisateur - trouvé en analysant le nouveau design): quand le DÉBORDEMENT DE SEGMENT lui-même déborde 16 bits (ES déjà à F000h-FFFFh, "add ax,1000h" donnerait 10000h), le segment reviendrait FAUSSEMENT à 0000h - la comparaison d'adresse physique suivante utiliserait alors une adresse ERRONÉE (physique 0 au lieu du "vrai" FFFFFh+1, un 21e bit que ce matériel ne peut pas représenter), concluant à tort qu'il faut continuer et provoquant une boucle infinie qui redump tout l'espace d'adressage en continu. Corrigé: le flag CF (retenue) de "add ax,1000h" est vérifié directement - s'il est posé, la plage maximale du matériel vient d'être entièrement couverte, le dump s'arrête immédiatement (avant même de faire le "mov es,ax" avec la valeur erronée).
  - Optimisation découverte en cours de route: BP peut adresser VAR_SEG DIRECTEMENT sans passer par ES, car SS = VAR_SEG (=STACK_SEG=1000h) EN PERMANENCE depuis l'initialisation de la pile au tout début de start: (jamais réassigné ensuite) - et l'adressage [BP+déplacement] utilise SS par défaut sur le 8086 (pas besoin de prefixe "ss:" explicite). Évite d'avoir à sauvegarder/restaurer ES (segment du dump en cours) autour de l'accès a la RAM partagée dans la boucle.
  - Vérifié en détail via listing d'assemblage (nasm -l), octet par octet: "add ax,1000h" + "jc" confirmé correct, "cmp dx,[bp]"/"cmp ax,[bp]" confirmés sans préfixe de segment (adressage SS implicite via BP). Trace manuelle complète du cas 0000:0000-F000:FFFF (65536 lignes, arrêt correct via le CF de l'avance de segment) ET d'un petit cas normal (0000:0000-0000:001F, 2 lignes, arrêt correct via la comparaison d'adresse physique). make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.
* Confirmé sur le matériel réel: le bug "0000" -> "5000" est corrigé (accumulateur SI dans ps2_read_hex_editable/ps2_edit_byte_value), et l'IVT est correctement initialisé (INT 10h/16h aux bonnes entrées, reste de l'IVT en RAM résiduelle comme attendu). Bug #2 ('q' ne fonctionne plus dans le DUMP) toujours non clarifié/non reproduit - en attente de précisions de l'utilisateur.
* Il y a beaucoup de garbage dans la mémoire. Au démarrage: 1) initialiser toute la RAM à 0. 2) initialiser tous les vecteurs de l'IVT avec un appel à une fonction qui affiche sur l'UART que l'interruption n'est pas implémentée. 3) Après, vient l'initialisation de nos vecteurs (INT 10h/16h). Réalisé:
  - solution-01.asm (start:): nouveau bloc EN LIGNE (pas de CALL) juste après l'init de SS/SP - efface les 128K de RAM (segments 0000h et 1000h) via "rep stosw" (AX=0 comme valeur de remplissage tout du long, CX sert de registre de transfert pour charger ES du 2e segment sans toucher à AX). Doit être en ligne (pas une procédure appelée): le segment 1000h contient la pile déjà active (SS/SP), et un retour d'appel qui s'y trouverait serait effacé par erreur - sans risque ici puisque rien n'a encore été empilé à ce stade du démarrage.
  - Nouvelles procédures init_ivt_not_implemented (peuple les 256 entrées de l'IVT avec int_not_implemented) et int_not_implemented (affiche "*** Interruption non implementee ***" sur l'UART via un appel DIRECT à uart_tx_string - pas la macro "print", qui passe par INT 10h - pour rester indépendant de ce qui pourrait être en cause si une interruption inattendue survient - puis IRET).
  - Ordre au démarrage confirmé: efface RAM -> init_8255/copie fantôme port A -> init_ivt_not_implemented (256 entrées) -> setup_bios_interrupts (remplace ENSUITE seulement les entrées INT 10h/16h par nos gestionnaires reels).
  - Vérifié en détail via listing d'assemblage (nasm -l), octet par octet: "rep stosw" (F3AB) confirmé aux 2 bons endroits, AX jamais modifié entre les deux, boucle de 256 itérations (CX=100h) confirmée dans init_ivt_not_implemented, adresse de int_not_implemented (0AA6h) confirmée correcte dans le pointeur FAR ecrit a chaque entree. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés.
* Confirmé sur le matériel réel: le Dump du premier 1 Ko (l'IVT) confirme la mise à zéro de la RAM (segment 0000h effacé avant d'être repeuplé) ainsi que la mise en place correcte de l'IVT (int_not_implemented partout sauf 0040h/0058h).
* Pour l'édition de la RAM: pouvoir spécifier une plage (taille en octets, discussion préalable via AskUserQuestion) et se déplacer avec les flèches haut/bas même si la plage déborde des 4 lignes d'affichage (défilement). Si Échap est appuyé, aucune modification n'est enregistrée; si 'q' est appuyé, les changements sont enregistrés. Limiter la taille de la plage à 1024 octets. Ne pas permettre de modifier l'IVT - proposer (en pratique: valider et rejeter, discussion préalable) une adresse de départ supérieure à l'IVT. Correction de l'adresse possible pendant la saisie (déjà le cas via ps2_read_hex_editable). Réalisé - refonte complète de edit_ram_action:
  - include/hardware.inc: zone réservée de VAR_SEG agrandie de 1 à 2 Ko pour loger un TAMPON DE TRAVAIL de 1024 octets (EDIT_BUFFER_OFF) - toute l'édition s'y fait, jamais directement en RAM réelle. Nouvel état: EDIT_SIZE_OFF (taille saisie), EDIT_CURSOR_OFF (position linéaire du curseur dans le tampon/la plage, remplace EDIT_ROW_OFF/EDIT_COL_OFF), EDIT_WINDOW_ROW_OFF (ligne logique affichée en haut, pour le défilement).
  - solution-01.asm (test_ram): nombre d'octets testés réduit de 130048 à 129024 (127→126 blocs) en conséquence de l'agrandissement de la zone réservée - tous les messages/textes concernés mis à jour (txt_ram_ok, txt_banniere2, lcd_txt_bloc_mid).
  - solution-01.asm (edit_ram_action): demande adresse de départ (rejetée si < 0x0400, message d'erreur) PUIS taille en octets (rejetée si 0, >1024, ou si depart+taille-1 déborde 0xFFFF) - reprend le pattern de validation déjà établi par dump_memory_action. edit_ram_load_buffer copie la plage réelle vers le tampon au démarrage; edit_ram_commit_buffer fait l'inverse, appelée UNIQUEMENT sur Q/q (jamais sur Échap - qui saute directement à la sortie sans rien recopier).
  - Nouvelles procédures: edit_ram_scroll_to_cursor (ajuste la fenêtre visible pour garder le curseur logique visible, réutilisée par move_up/move_down/advance), edit_ram_draw_grid (réécrite: lit le TAMPON au lieu de la RAM réelle, calcule la ligne logique de chaque ligne visible via fenêtre+déplacement, gère les lignes/colonnes au-delà de la plage réelle avec des espaces de remplissage), edit_ram_cell_ddram (réécrite: position DDRAM dérivée du curseur linéaire ramené à sa position VISIBLE via la fenêtre).
  - Astuce découverte: BP adresse VAR_SEG directement via SS (= VAR_SEG en PERMANENCE depuis l'init de la pile, jamais réassigné ensuite) - [BP] utilise SS par défaut sur le 8086, évite de jongler avec ES pour les nombreux accès aux nouvelles variables partagées.
  - DEUX bugs trouvés et corrigés AVANT déploiement par relecture attentive du listing ET trace manuelle (méthodologie renforcée pour cette fonctionnalité, la plus complexe de la session): 1) edit_ram_draw_grid réutilisait BX (fenêtre visible) ET CX (taille totale) d'une itération de ligne à l'autre SANS les relire depuis la RAM - BX se faisait écraser par le nombre de colonnes valides de la ligne précédente, corrompant le calcul de la ligne suivante. Corrigé: BX/CX sont relus depuis EDIT_WINDOW_ROW_OFF/EDIT_SIZE_OFF au début de CHAQUE ligne. 2) edit_ram_move_down, pour une plage tenant sur une seule ligne partielle (taille < 6), sautait du début à la fin de CETTE MÊME ligne au lieu de ne rien faire (aucune ligne existe en dessous) - corrigé en vérifiant D'ABORD qu'une ligne suivante existe réellement (même partielle) avant de calculer la nouvelle position.
  - Vérifié en détail via listing d'assemblage (nasm -l), octet par octet, PUIS trace manuelle complète de plusieurs scénarios (édition simple sans défilement, défilement vers le bas puis vers le haut à travers plusieurs lignes, dernière ligne partielle avec colonne dépassant sa largeur réelle - clampée correctement, plage d'un seul octet). make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel (fonctionnalité la plus ambitieuse et la plus à risque de la session).
* Bug rapporté sur le matériel réel: dans Edit RAM, lors du déplacement, le premier caractère de l'adresse des deux premières lignes n'est pas affiché. Diagnostic: chaque ligne du LCD affichait une étiquette d'adresse ("SSSS: ", 6 caractères, ajoutée lors de la refonte précédente pour aider à s'orienter pendant le défilement) + la grille compacte (6 cases "XX " = 18 caractères) = 24 caractères au total, DÉPASSANT LA LARGEUR DE 20 CARACTÈRES DU LCD DE 4. Sur cet afficheur 4x20 "type A", LCD_LINE1<->LCD_LINE3 et LCD_LINE2<->LCD_LINE4 partagent chacun un même bloc de 40 octets de DDRAM (l'auto-incrémentation boucle à l'intérieur de CE bloc) - le débordement de 4 caractères d'une ligne (ex: LCD_LINE1) se retrouve donc au DÉBUT de sa ligne pairée (LCD_LINE3), et puisque LCD_LINE3 est dessinée JUSTE APRÈS dans la boucle de edit_ram_draw_grid, SON PROPRE débordement de 4 caractères boucle à son tour et écrase le DÉBUT de LCD_LINE1 (l'étiquette d'adresse qu'on venait de dessiner correctement) - même mécanique pour LCD_LINE2/LCD_LINE4. Corrigé: l'étiquette d'adresse par ligne est retirée du LCD (gardée sur l'UART uniquement, où la largeur n'est pas contrainte) - la grille LCD retrouve son format compact d'origine (18 caractères, tient dans les 20 disponibles). edit_ram_cell_ddram (qui avait été modifiée en même temps pour tenir compte de cette étiquette, +6 au décalage colonne) est revenue à sa formule d'origine (col*3, sans décalage). Cette omission (compter la largeur totale d'une ligne LCD avant de l'écrire) est exactement le genre de vérification que le reste du projet fait déjà systématiquement (dump_line, msg_bloc_progression: toutes les lignes LCD sont délibérément complétées à EXACTEMENT 20 caractères, jamais plus) - à appliquer plus rigoureusement pour toute future modification touchant l'affichage LCD par ligne. Vérifié via listing d'assemblage. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés.
* Confirmé sur le matériel réel: Edit RAM fonctionne correctement (adresse/taille, grille, défilement, édition, Q/Échap) - le correctif de l'étiquette d'adresse LCD est validé.
* Dans le sous-menu Dump memory, ajouter une option qui affiche le contenu des registres du 8088, sur le LCD et l'UART. Pour l'UART, produire un affichage ergonomique, incluant la valeur des Flags. La touche ESC permet de revenir au menu précédent. Réalisé - registers_dump_action (option 3 du menu Dump memory):
  - Capture immédiate à l'entrée (avant tout usage des registres généraux comme espace de travail): AX/BX/CX/DX/SI/DI empilés, plus CS/DS/ES/SS/FLAGS (empilés seulement pour lecture, jamais restaurés dans un registre - ils ne sont jamais modifiés par cette routine). BP fixé juste après "push bp"/"mov bp,sp" (même "prologue de trame de pile" qu'un compilateur C) - les valeurs empilées AVANT ce point (BP original, adresse de retour empilée par le CALL) restent à des décalages POSITIFS malgré tous les push qui suivent, puisque BP lui-même ne bouge plus. IP affiché = l'adresse de retour déjà empilée par le CALL qui a mené ici (équivalent exact de ce qu'un débogueur montrerait à un point d'arrêt juste après ce CALL). SP affiché = celui vu par l'appelant AVANT ce CALL (BP+4, simple calcul, jamais relu depuis la pile).
  - UART: tout d'un coup, format inspiré de DEBUG.COM (le débogueur DOS classique) - nouvelle macro uart_flag_bit (3 paramètres: masque, étiquette si bit=1, étiquette si bit=0) invoquée 8 fois pour décoder FLAGS en mnémoniques classiques (OV/NV DN/UP EI/DI NG/PL ZR/NZ AC/NA PE/PO CY/NC, ordre OF DF IF SF ZF AF PF CF) - le mnémonique "actif" (bit=1) coloré en jaune pour ressortir à l'oeil.
  - LCD: 14 valeurs à afficher, trop pour les 4x20 caractères disponibles d'un coup - PAGINE sur 2 écrans (Gauche/Droite pour basculer, comme edit_ram_action). Page 1: AX/BX/CX/DX/SI/DI/SP/BP. Page 2: CS/IP/DS/ES/SS/FL, puis ligne 4 = les 8 FLAGS décodés en UNE LETTRE chacun (O D I S Z A P C), MAJUSCULE si le bit est actif, minuscule sinon (+20h, motif standard ASCII maj->min) - complétée à exactement 20 caractères (5 espaces de remplissage) pour écraser tout résidu de la page précédente. Numéro de page vivant dans SI (jamais touché par gotoxy/print/lcd_tx_hex_word/lcd_data/ps2_get_char, tous le préservent - la valeur ORIGINALE de SI, elle, a déjà été affichée via UART et relue depuis la pile avant que SI ne serve de numéro de page).
  - Chaque ligne du LCD (page 1 ET 2) vérifiée pour NE PAS dépasser 20 caractères (leçon retenue du bug d'Edit RAM: étiquette d'adresse qui débordait et corrompait la ligne pairée) - ex. ligne1 page1: "AX=XXXX" (0-6) + "BX=XXXX" (9-15) + "1/2" (17-19) = exactement 20, aucun débordement.
  - Menu Dump memory renuméroté: "1) Dump memory / 2) Edit RAM / 3) Registres CPU / 9) Home menu".
  - Vérifié en détail via listing d'assemblage (nasm -l), octet par octet: tous les décalages [bp-N]/[bp+N] confirmés corrects (ex: "8B46F4" = mov ax,[bp-12]), séquence de restauration finale confirmée (pop di/si/dx/cx/bx/ax puis "add sp,10" pour libérer FLAGS/SS/ES/DS/CS SANS les repop-er dans un registre, puis pop bp, ret). make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.
* Question: quelle touche pour changer de page LCD dans Registres CPU? Réponse: Flèche gauche OU droite (les deux basculent, peu importe la direction) - voir registers_dump_action, .toggle_page.
* Pour l'affichage des registres sur UART, afficher aussi les valeurs en binaire et afficher les flags sur une ligne individuelle. Réalisé:
  - lib/uart.asm: nouvelle procédure uart_tx_bin_word (affiche AX en binaire, 16 caractères '0'/'1', MSB en premier - même discipline de préservation de registres que uart_tx_hex_*).
  - solution-01.asm: nouvelle procédure print_reg_hex_bin_uart (affiche AX en hexa PUIS en binaire, séparés par 2 espaces - le libellé "AX=" etc. est affiché séparément par l'appelant via la macro "print", avant l'appel).
  - registers_dump_action (section UART) entièrement réorganisée: 2 registres par ligne (AX/BX, CX/DX, SI/DI, SP/BP, DS/ES, SS/CS, IP seul), chacun en hexa+binaire, au lieu de l'ancien format "tout sur 2 lignes, hexa seulement". FLAGS déplacé sur SA PROPRE ligne (après une ligne vide de séparation), avec hexa+binaire+mnémoniques (au lieu d'être accolé à la fin de la ligne IP/segments comme avant). Textes txt_reg_* simplifiés (retrait des espaces de tête qui géraient l'ancien alignement sur 1 ligne, remplacés par un nouveau séparateur explicite txt_reg_pair_sep entre les 2 registres d'une même ligne).
  - Vérifié via listing d'assemblage (nasm -l), octet par octet (uart_tx_bin_word, print_reg_hex_bin_uart). make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.* Dans le menu Dump memory, ajouter une option qui permet d'éditer la RAM à partir du deuxième bloc de 64K (1000:0000) et d'exécuter, via la touche 'r', le code qui y a été saisi (routine se terminant par RET/RETF), affichant les registres sur l'UART après exécution. Remplacer l'option "9) Home menu" par un simple appui sur Échap (non affichée à l'écran). Réalisé - edit_run_action (option 4 du menu Dump memory):
  - Réutilise TOUT l'appareil de edit_ram_action (edit_ram_draw_grid/cell_ddram/place_cursor/write_current/scroll_to_cursor/move_left/right/up/down/advance) sans aucune modification: ces routines sont agnostiques du segment, elles ne touchent jamais à la "vraie" RAM, seulement au tampon EDIT_BUFFER_OFF partagé. Seules deux nouvelles routines, edit_run_load_buffer/edit_run_commit_buffer, sont des copies strictement identiques de edit_ram_load_buffer/commit_buffer sauf que le côté "RAM réelle" vise le segment 1000h (VAR_SEG) au lieu de 0000h.
  - Pas de saisie d'adresse (contrairement à Edit RAM): l'adresse de départ est TOUJOURS 0000h (donc 1000:0000), fixée directement dans EDIT_BASE_OFF - seule la taille est demandée (1-1024 octets, mêmes limites qu'Edit RAM via EDIT_MAX_SIZE), largement sous la zone réservée (0F800h-0FFFFh) et sous la pile active (SS=1000h), donc sans risque de chevauchement.
  - Touches: Échap annule (comme Edit RAM); Q/q enregistre le tampon dans la RAM réelle SANS exécuter; R/r enregistre PUIS exécute (edit_run_execute_and_show) et affiche les registres.
  - edit_run_execute_and_show: exécute le code injecté via un CALL FAR IMMÉDIAT ("call 1000h:0000h", opcode 9A encodé directement par NASM) - le code injecté DOIT se terminer par RETF (jamais un RET proche, qui laisserait CS empilé et planterait la carte au retour, puisqu'un CALL FAR empile IP ET CS). CALL/RETF ne modifient jamais un registre général ni FLAGS: immédiatement après le retour, chaque registre empilé (AVANT tout autre usage) reflète exactement ce que le code injecté a laissé - même technique de capture par trame de pile que registers_dump_action ("push bp"/"mov bp,sp" puis pushf/push ss/es/ds/cs/ax/bx/cx/dx/si/di, relecture via [bp±N]), mais avec un seul mot de décalage avant l'ancrage (pas de "CALL qui a mené ici" à comptabiliser ici, contrairement à registers_dump_action): SP tel que laissé par le code injecté = bp+2 (au lieu de bp+4). Affichage UNIQUEMENT sur l'UART (demande explicite - pas de LCD), même format hexa+binaire+FLAGS sur sa propre ligne que registers_dump_action (réutilise print_reg_hex_bin_uart/txt_reg_*/uart_flag_bit). IP non affiché (aucune signification utile ici).
  - Risque documenté (comme la commande "G" de DEBUG.COM): si le code injecté modifie SS sans le restaurer, l'affichage des registres qui suit ciblerait une pile invalide - inhérent à l'exécution de code arbitraire, non protégeable côté firmware.
  - Menu Dump memory: "9) Home menu" retiré (LCD ET UART) - Échap fait maintenant office de retour au menu principal sans être affiché à l'écran. Nouvelle option "4) Edit+Run RAM" affichée à sa place.
  - Vérifié en détail via listing d'assemblage (nasm -l), octet par octet: "call 1000h:0000h" confirmé encodé en CALL FAR immédiat (9A 0000 0010), tous les décalages [bp±N] de edit_run_execute_and_show confirmés corrects (ex: "8B46F4" = mov ax,[bp-12], "83C002" = add ax,2 pour SP), edit_run_load_buffer/commit_buffer confirmées identiques aux originales sauf ES=VAR_SEG au lieu de 0, dispatch .dump_4/.dump_esc confirmé. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.
* Pour Edit+Run RAM: 1) pouvoir appuyer sur 'r' PENDANT la saisie d'un octet (pas seulement entre deux cases); 2) éliminer la nécessité d'appuyer sur Entrée pour valider la saisie d'un octet; 3) ne plus demander le nombre d'octets à saisir, fixer à 255. Réalisé:
  - Diagnostic du point 1: ps2_edit_byte_value (lib/ps2.asm, PARTAGÉE avec edit_ram_action - inchangée) boucle INTERNEMENT sur ps2_get_char jusqu'à Entrée, touche par touche, sans jamais rendre la main à l'appelant avant - ce qui avalait silencieusement 'r'/'R' s'il était tapé APRÈS un premier chiffre (la boucle .wait_key extérieure d'edit_run_action, qui le reconnaît, elle, n'était jamais re-atteinte).
  - Nouvelle procédure edit_run_byte_value (solution-01.asm, DÉDIÉE à edit_run_action - ps2_edit_byte_value n'est pas touchée, edit_ram_action garde son comportement inchangé): même structure que ps2_edit_byte_value, avec deux ajouts - 'r'/'R' vérifiée EN PREMIER à chaque touche lue (retour immédiat via une 3e valeur de sortie DL=1, chiffre(s) partiel(s) ABANDONNÉS - rien n'est écrit pour la case en cours), et validation AUTOMATIQUE dès le 2e chiffre hexa (Entrée reste disponible pour valider un octet d'UN SEUL chiffre, mais n'est plus nécessaire pour un octet complet).
  - edit_run_action (.not_down): appelle edit_run_byte_value au lieu de ps2_edit_byte_value, teste DL=1 EN PREMIER (avant le test CF de "rien tapé") pour aiguiller vers .commit_and_run si 'r'/'R' a interrompu la saisie.
  - Point 3: suppression complète du prompt "Size:" (ps2_read_hex_editable, lcd_show, validation/rejet) - EDIT_SIZE_OFF fixé directement à EDIT_RUN_SIZE (255, nouvelle constante) en même temps que EDIT_BASE_OFF=0, sans aucune saisie: la grille s'affiche immédiatement après lcd_init. Le texte LCD "Run: 1000:0000" (devenu inutile, la grille prenant l'écran immédiatement) et son étiquette lcd_txt_run_addr ont été retirés (code mort).
  - Vérifié en détail via listing d'assemblage (nasm -l), octet par octet: la nouvelle procédure edit_run_byte_value confirmée byte-à-byte fidèle à ps2_edit_byte_value sauf les deux ajouts documentés, ordre des tests DL=1 puis CF dans edit_run_action confirmé correct, "mov word [es:di], EDIT_RUN_SIZE" confirmé écrire 255 (FF 00). make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.
* Bug rapporté sur le matériel réel: dans Edit+Run RAM, en tapant "B8" à l'adresse de départ, le 'B' s'affiche mais dès que '8' est tapé, l'écran se réinitialise et l'octet reste à 00. Diagnostic: dans edit_run_action (.not_down), la séquence "call edit_run_byte_value / cmp dl,1 / je .commit_and_run / jc .redraw" testait CF (jc) APRÈS avoir exécuté "cmp dl,1" - qui ÉCRASE le carry flag que edit_run_byte_value venait de positionner comme signal de sortie ("CF=0" = valeur prête). Puisque DL=0 dans ce cas (valeur composée avec succès), "cmp dl,1" calcule 0-1 (emprunt), mettant CF=1 par accident - donc "jc .redraw" se déclenchait à tort, jetant systématiquement la valeur composée dès le 2e chiffre auto-validé, sans jamais écrire le tampon. Corrigé: le test "jc .redraw" est déplacé AVANT "cmp dl,1" (l'ordre inverse ne pose pas ce problème, puisqu'on n'a plus besoin du CF original une fois qu'on a décidé de ne PAS aller à .redraw). Vérifié via listing d'assemblage (nasm -l): jc .redraw confirmé immédiatement après le call, avant cmp dl,1. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés.
* Ajout demandé: afficher l'adresse en tête de chaque ligne de la grille d'édition (Edit RAM ET Edit+Run RAM, code de rendu partagé) pour ne pas se perdre en défilant. Réalisé - EDIT_COLS réduit de 6 à 5 (nouvelle constante EDIT_ADDR_LABEL_WIDTH=5) pour degager EXACTEMENT la place d'une étiquette "SSSS:" (5 caractères, sans espace) en tête de chaque ligne LCD sans dépasser les 20 caractères disponibles (5 + 5 colonnes × 3 caractères "XX " = 20 EXACTEMENT) - reprend la leçon du bug précédent (ancien format 6 colonnes + étiquette = 24 caractères, débordait dans la ligne DDRAM pairée):
  - edit_ram_draw_grid: le calcul de décalage de ligne logique utilise maintenant EDIT_COLS (au lieu du littéral 6); l'adresse réelle de chaque ligne (déjà calculée pour l'UART) est maintenant AUSSI affichée sur le LCD (lcd_tx_hex_word + ':' via lcd_data) AVANT l'affichage UART - AX préservé entre les deux via push/pop, puisque les deux détruisent AX.
  - edit_ram_cell_ddram: le diviseur (ligne logique/colonne) utilise EDIT_COLS au lieu de 6; le calcul final de l'adresse DDRAM ajoute maintenant EDIT_ADDR_LABEL_WIDTH (5) en plus du décalage de colonne, pour pointer la case après l'étiquette.
  - Toutes les autres routines partagées (place_cursor, write_current, move_left/right/up/down, advance, ps2_edit_byte_value, edit_run_byte_value) restent INCHANGÉES - elles utilisent déjà EDIT_COLS symboliquement ou l'adresse DDRAM absolue (AH) sans jamais recalculer la largeur elles-mêmes.
  - Vérifié en détail via listing d'assemblage (nasm -l), octet par octet: EDIT_COLS=5 confirmé partout (B90500/BA0500 = mov cx/dx,5), "add al, EDIT_ADDR_LABEL_WIDTH" confirmé (0405 = add al,5), séquence push ax/lcd_tx_hex_word/lcd_data(':')/pop ax/uart_tx_hex_word confirmée avant le calcul des colonnes valides. make all/check/check-modules + build TEST_PS2 + build TEST_I2C_DUMP tous validés. Pas encore testé sur le matériel réel.
